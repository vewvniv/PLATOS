## 1. A pergunta que dimensiona o resto

- [x] 1.1 Verificar o que `supabase-kt` reporta para credencial recusada e para ausência de rede, num teste de ligação mínimo. Resultado: ou os dois casos são distinguíveis pelo que a biblioteca devolve, ou não são — e a resposta fica escrita antes de qualquer tela existir.
- [x] 1.2 Se não forem distinguíveis, a distinção vem **do lado da falha** — exceção de transporte contra resposta 401, desembrulhando a causa —, e **nunca** de conferir a conectividade antes de tentar. Conferir antes é estado no instante da checagem: a rede cai no meio, a checagem já passou, e o erro sai rotulado como credencial recusada. Z3 quebraria de forma intermitente, que passa no teste e falha na sala. Registrar a saída escolhida no `design.md`. Se forem distinguíveis, registrar isso também: verificação que confirmou o esperado ainda é registro.

## 2. Dependências e configuração

- [x] 2.1 Declarar `ktor-client` em `gradle/libs.versions.toml` e `apps/android/build.gradle.kts`, com a versão do Ktor referenciada de um lugar só. **`supabase-kt` não entra** — decisão 9, fechada com a medição da decisão 8. Resultado: `./gradlew :apps:android:assembleDebug` verde, APK registrado. O número que a 3c anotaria não existe; a linha de base é a medição de 2026-09-02, e o APK **subiu** 6 923 525 bytes, porque o cliente HTTP entra no aplicativo pela primeira vez e `auth-kt` nunca esteve nele.
- [x] 2.2 Campos de `BuildConfig` para URL do Supabase, chave anônima e URL da API, alimentados por propriedades de build não versionadas, com valores ausentes falhando o build com mensagem que diz qual falta. Resultado: nenhum segredo no repositório, e ambiente errado não passa em silêncio. Resolução `-P` → ambiente → `local.properties`; o CI alimenta por ambiente, com marcadores em `.invalido`. Três defeitos vistos falhar em `docs/cobertura-fatia-4a-zero.md`, e `ConfiguracaoTest` afirma do outro lado que o valor atravessou até `BuildConfig` na forma que quem chama assume.

## 3. A sessão, sem rede

- [x] 3.1 Implementar `DeviceSession` como máquina de estados em Kotlin puro, dirigida por resultados montados à mão — autenticou, organizações chegaram, recusa por credencial, por rede, por sessão. Sem `supabase-kt` e sem cliente HTTP. Resultado: testável na JVM.
- [x] 3.2 Testar os três estados de falha distintos: credencial recusada, sem rede, sessão expirada. Resultado: os três cenários da spec passam, e nenhum é apresentado como o outro.
- [x] 3.3 Testar que o nome da organização só existe quando veio da consulta, e que consulta falha não produz nome nenhum. Resultado: os dois cenários da spec passam.
- [x] 3.4 **Ver falhar:** fazer a sessão cair num nome de reserva quando a consulta falha, e confirmar que 3.3 fica vermelho; reverter. Registrar. Nome inventado na tela é indistinguível de nome verdadeiro para quem lê, e essa é a forma de falha silenciosa desta fatia.
- [x] 3.5 Testar a escolha de organização: duas pedem escolha, uma só não pede, e a escolha sobrevive. Resultado: os três cenários da spec passam.
- [x] 3.6 Testar que sair apaga sessão **e** organização escolhida, e que a entrada seguinte não vem pré-selecionada para nenhum usuário. Resultado: os dois cenários de saída da spec passam.
- [x] 3.7 **Ver falhar:** fazer o logout apagar só a sessão e confirmar que 3.6 fica vermelho; reverter. Registrar. **É o defeito mais caro desta fatia** — aparelho compartilhado entre escolas, e o segundo usuário herda a organização do primeiro sem nada na tela dizendo isso.

- [x] 3.8 `aoConsultarOrganizacoes` passa a ignorar resultado que chega fora de `Consultando`, no mesmo padrão que `escolher` já usa com `Escolhendo`. Resultado: o 401 que o interceptador já transformou em sessão expirada não é sobrescrito pelo retorno da mesma chamada, e resultado que chega depois de sair não ressuscita a tela. **A alternativa era a fiação nomear 401**, que é o defeito da 4.4 entrando pela porta dos fundos: a regra é de estado, e mora onde o estado mora.
- [x] 3.8b **Ver falhar:** remover a guarda e confirmar que o cenário fica vermelho; reverter. Registrado. A mutação derruba **os dois cenários novos e só eles** — os treze anteriores ficam verdes, mesma leitura da 4.4 e da 5.4b.

## 4. A rede

- [x] 4.1 Ligar a autenticação por e-mail e senha em `ktor-client` (e não `supabase-kt` — decisão 9), entregando à sessão o resultado já destilado nos casos da tarefa 1.1. Resultado: `AutenticacaoSupabase` traduz e não decide; `Retorno` classifica num ponto único, compartilhado com a tarefa 4.2. Dez cenários de JVM com `MockEngine`, e três defeitos vistos falhar em `docs/cobertura-fatia-4a-zero.md`.
- [x] 4.2 Cliente HTTP para a API, com a credencial da sessão em cada chamada, e o DTO de organização compartilhado com o servidor. Resultado: `ApiPlatos` consulta `/me/organizations` com a credencial posta por `defaultRequest` — e não pelo método que chama, para que o segundo endpoint não tenha o que esquecer. O 401 chega **cru** para a tarefa 4.3, e traduzi-lo aqui seria o defeito da 4.4. **"Compartilhado" virou espelho, e não arquivo compartilhado**: os dois módulos dependem de `packages:domain`, então dividir o DTO era possível, mas a proposal e a 7.4 declaram `packages/domain` e `apps/api` intocados; o contrato é repetido e a deriva é fechada pelo JSON literal em `ApiPlatosTest`. Onze cenários de JVM com `MockEngine`, e três defeitos vistos falhar em `docs/cobertura-fatia-4a-zero.md`.
- [x] 4.3 Interceptador que leva qualquer 401 ao estado de sessão expirada, num ponto único. Resultado: `clienteApi` instala um `HttpResponseValidator` — e não um `ResponseObserver`, que rodaria numa corrotina à parte e deixaria a tela desenhar antes de a sessão saber. É função própria, e não um bloco dentro de `ApiPlatos`, para que "nenhuma tela trata 401" seja verificável sobre o cliente: os testes batem em rotas que não têm método nenhum. 403, 500 e falha de transporte não mexem na sessão, cada um com seu cenário.
- [x] 4.4 **Ver falhar:** tratar o 401 dentro do chamador em vez do interceptador, acrescentar uma segunda chamada autenticada que não o trate, e confirmar que a expiração passa a escapar por ela; reverter. Registrar. Feito, e o achado não foram os três testes que ficaram vermelhos: foi `ApiPlatosTest` inteiro continuar **verde**, e o teste da segunda chamada passar afirmando que a sessão não expirou. Suíte escrita só contra o adaptador teria aprovado a versão defeituosa por unanimidade. Registrado em `docs/cobertura-fatia-4a-zero.md`.
- [x] 4.5 Guardar a sessão com `EncryptedSharedPreferences` sobre chave do Keystore, e a organização escolhida no armazenamento privado comum, com backup automático desabilitado para as duas. Resultado: `SessaoGuardadaAndroid` com os dois armazenamentos separados, e `allowBackup="false"` no aplicativo inteiro — em vez de regra de exclusão, que silencia quando alguém acrescenta o terceiro arquivo. **A API está depreciada**, e a revisão de 2026-09-03 da decisão 5 registra por que ela fica e qual o gatilho para reabrir; o custo aceito é o keyset corrompido virar responsabilidade desta base, tratado como sessão inválida em vez de exceção, com nove cenários de JVM e duas mutações em `docs/cobertura-fatia-4a-zero.md`. **Os dois cenários de credencial em repouso da spec ainda NÃO passam** — eles precisam de aparelho, e são a 4.6.
- [x] 4.6 **Ver falhar:** guardar a sessão em claro e confirmar que o cenário "a credencial não está em claro" fica vermelho; reverter. Registrar. Feito no `platos-atd34`, com quatro mutações em vez de uma: em claro, em Base64, `allowBackup` de volta, e a busca apontada para `cacheDir`. A frase da tarefa estava certa e faltava uma volta — o teste grava também o identificador da organização, que fica em claro de propósito, e **exige achá-lo** antes de afirmar que não acha o token. Foi essa guarda que a quarta mutação derrubou: sem ela o cenário passaria sem nada ter sido examinado. Uma segunda guarda entrou depois, por revisão: a primeira versão sincronizava com a escrita da *organização* e não com a da *credencial*, e passava por causa da fila FIFO de `apply()` — detalhe não declarado que trocar duas linhas destruiria. Agora o arquivo cifrado é fotografado antes e o teste espera os bytes mudarem, e a mutação da ordem trocada é verde de propósito. Seis mutações no total. Os dois cenários de credencial em repouso da spec passam.

- [x] 4.7 Instalar `HttpTimeout` no cliente HTTP. Sem ele, servidor que aceita a conexão e nunca responde deixa `Consultando` para sempre — o que a spec proíbe ao dizer que o aplicativo não fica em carregamento sem desfecho. `Retorno` já foi escrito para isto: o comentário dele cita `HttpRequestTimeoutException` entre os `IOException` que viram `SemRede`, então nenhuma classificação nova entra. Teste na JVM com `MockEngine` atrasando a resposta — texto puro e não JSON, para que remover o plugin falhe dizendo que veio `Respondeu` onde se esperava `SemRede`, e não por desserialização. **O número é provisório: 90 s, não medido contra o nosso servidor.** Base: cold start do plano gratuito do Render fica tipicamente entre 30 e 60 s, e 90 s dá folga sobre o pior caso relatado. Errar para baixo é o erro caro — cold start interrompido chega como `IOException`, vira `SemRede`, e o professor lê "confira a conexão" com a conexão boa. Um terceiro cenário afirma o **piso** de 60 s, para que ninguém baixe o número sem enfrentar isso. Duas mutações vistas falhar.

## 5. As telas

- [x] 5.1 Tela de entrada em Compose: e-mail, senha, e a faixa de motivo em cada um dos três estados de falha. **A escolha da frase saiu da tela**: `mensagemDeEntrada` é função pura, com `when` exaustivo sem `else`, então motivo novo quebra a compilação em vez de cair em texto genérico. Três cenários de JVM — toda mensagem existe, nenhuma se repete, e primeira abertura devolve `null` e não string vazia, que desenharia faixa em branco. Três mutações vistas falhar. O que fica sem teste automático é a faixa **aparecer**, que é aparelho.
- [x] 5.2 Tela de escolha de organização, apresentada só quando houver mais de uma. `EscolhaScreen` desenha `DeviceState.Escolhendo` e nada mais — quem decide que há escolha a fazer é `DeviceSession`, e os três cenários disso são da 3.5, já verdes. Sem ordenação própria: a ordem é a que o servidor mandou, e reordenar aqui faria a tela discordar do que `ApiPlatosTest` afirma.
- [x] 5.3 Tela de trabalho mostrando o nome da organização ativa e oferecendo sair, com a navegação para o escaneamento. `TrabalhoScreen` desenha o nome que veio da API, sem nome de reserva nem texto de espera no lugar dele — quando a consulta não devolve, o estado não é `Ativa` e esta tela não é alcançada (3.4). Oferece as ações; quem apaga as duas coisas ao sair é `DeviceSession.sair()` (3.7).
- [x] 5.0 Mover `android.permission.INTERNET` do manifesto de `debug` para o principal. Feito, e o defeito foi **medido antes**: o manifesto mesclado de release trazia `CAMERA` e `DUMP` e não `INTERNET`; depois da mudança traz as três, e o `debug` continua com ela por herança, então o probe segue funcionando. O manifesto de `debug` foi removido — a permissão era todo o conteúdo dele.
- [x] 5.4 Tela para `DeviceState.SemOrganizacao`: explica que não conseguiu obter a organização, e **não apresenta nome nenhum**. `SEM_REDE` e `OUTRA` dizem coisas diferentes, pela mesma razão que os três motivos de entrada dizem. Escrita como tarefa própria porque a seção 5 nomeava tela para quatro dos cinco estados, e a spec exige este — sem ele a tela teria de escolher entre voltar à entrada mentindo que a credencial é o problema, ou mostrar trabalho sem nome.
- [x] 5.4b **Ver falhar:** feito em três formas, com `DeviceSession` intocado e verde nas três. Nome de reserva no título e `"Carregando..."` no lugar da explicação ficam vermelhos. **A terceira não é pega**: a tela ignorando o parâmetro e escrevendo um literal passa por toda verificação de JVM, porque nenhum teste desta base lê o que o `@Composable` desenha. Fica registrada como lacuna conhecida em `docs/cobertura-fatia-4a-zero.md`, não como risco mitigado.
- [x] 5.5 A entrada vira o `Activity` de lançamento; `ScanActivity` passa a ser alcançada a partir da tela de trabalho e continua com o `assets.open` provisório. `SessaoActivity` liga adaptadores e escolhe tela, e nada mais. O `when` estado→tela é **exaustivo sem `else`** — remover um ramo quebra a compilação nomeando o que falta, visto. O 401 deixou de precisar de tratamento aqui: interceptador detecta, a ordem garante que a expiração chega antes do retorno (dois cenários novos em `ClienteApiTest`), e a guarda da 3.8 descarta o atrasado. `paraSessao()` mapeia `Recusou` sem olhar status. Conferido no manifesto mesclado: `SessaoActivity` `exported=true` com `LAUNCHER`, `ScanActivity` `exported=false` sem `intent-filter`.

## 6. Conferência em aparelho — depende do mantenedor

- [ ] 6.1 Entrar com credencial válida num projeto Supabase real e conferir que o nome apresentado é o que a API devolveu. Resultado: o caminho inteiro fecha fora do teste.
- [ ] 6.2 Conferir os três estados de falha à mão: credencial errada, aparelho em modo avião, e sessão invalidada do lado do servidor. Resultado: as três dizem coisas diferentes, e nenhuma fica carregando sem desfecho. **Dois de três conferidos** no `platos-atd34`, contra o projeto Supabase real: credencial inexistente → "E-mail ou senha nao conferem", permanecendo na entrada com a senha mascarada; modo avião com a **mesma** credencial nos campos → "Nao foi possivel falar com o servidor". Nenhuma apresentada como a outra. Falta o terceiro — sessão invalidada do lado do servidor —, que exige sessão válida e portanto usuário real.
- [ ] 6.3 Forçar a consulta a falhar depois de autenticar — modo avião logo após a entrada fechar — e conferir que a tela de `SemOrganizacao` apresenta **só a explicação**: nenhum nome, nenhum placeholder, nenhum texto de espera parado. Resultado: o caso que nenhum teste de JVM alcança fica visto. É o par em aparelho da lacuna registrada na 5.4b, onde a tela ignorando o parâmetro e escrevendo um literal passa por toda a suíte.
- [ ] 6.4 Com um usuário em duas organizações, escolher uma, fechar o aplicativo e reabrir. Resultado: a escolha sobreviveu e não foi pedida de novo.
- [ ] 6.5 Sair, entrar com **outro** usuário no mesmo aparelho, e conferir que nenhuma organização vem pré-selecionada. Resultado: o defeito da tarefa 3.7 não acontece no aparelho.
- [ ] 6.6 Medir o pior caso de resposta contra o serviço real — incluindo cold start depois de o Render suspender o serviço — e fixar `TEMPO_LIMITE_DE_PEDIDO_MS` a partir dessa medição, substituindo os 90 s provisórios. Registrar a relação entre o número e o medido. Está aqui, e não na seção 4, porque depende do mesmo serviço que 6.1, 6.2 e 6.3 já exigem: montar o painel uma vez serve a todas. **Esperado, e não surpresa:** 30–60 s de cold start dispara o ponto-limite da decisão 10, que manda reabrir a reconsulta em toda recriação se a resposta passar de 10 s. O registro está fazendo o trabalho dele; a decisão de agir é de quem fizer a seção 6.
- [ ] 6.7 Acrescentar a `docs/protocolo-medicao-impressa.md` a seção de conferência da entrada, com esses passos. Resultado: quem repetir depois não reinventa o roteiro.

## 7. Verificação final

- [x] 7.1 **Reaberta e fechada de novo em 2026-09-04.** Rodar `./gradlew build` e os testes de JVM do módulo Android. Resultado: verde, com `lintDebug` incluído. Conferido que `lintDebug` e `testDebugUnitTest` estão **no grafo** de `build` (`--dry-run`) e que executaram de fato: rodada com `--rerun-tasks`, **173 de 173 tarefas executadas**, nenhuma `UP-TO-DATE`. 109 testes no Android, 107 na API, 299 em `packages/domain` (jvmTest), zero falhas. Onze avisos do compilador, **todos pré-existentes** em arquivos de teste de `apps/api` e `packages/domain`, e zero em `apps/android` — a rodada incremental os escondia, porque nada recompilava.
  **Por que reabriu, e por que fechou.** O CI acusou `paridade` vermelha na PR #30, com paridade
  verde nas quatro execuções anteriores de `main`. A causa **não era regressão**: o workflow tem
  `concurrency: cancel-in-progress: true`, e o push seguinte cancelou a execução em andamento — o
  passo aparece como falha, e o log dele não tem erro nenhum, só limpeza de pós-job. A execução do
  commit seguinte fechou verde nos três jobs.
  **O que a investigação achou de verdade** foi outra coisa, e essa era real: `./gradlew build`
  **não** roda a suíte instrumentada, e localmente ela só tinha sido executada com filtro de classe.
  A 7.1 afirmava um verde que não cobria o que o CI cobre. Corrigido rodando
  `./gradlew :apps:android:connectedDebugAndroidTest` sem filtro no `platos-atd34` — mesma imagem do
  CI, conferida (`android-34/aosp_atd/x86_64`, `pixel_6`): **46 testes, zero falhas**.
- [x] 7.2 Completar `docs/cobertura-fatia-4a-zero.md` com como cada verificação crítica foi vista falhar, **e com o que ficou sem teste automático e por quê** — o interceptador de 401, a cifragem em repouso, a configuração e o corpo de sucesso da autenticação. **A lista envelheceu, e o registro diz isso**: os três primeiros fecharam, sempre pelo mesmo movimento — tirar a decisão de dentro do adaptador. Só o corpo de sucesso continua descoberto, porque ele não é encanamento e sim um fato sobre servidor de terceiro. Duas lacunas novas entraram, e as duas apareceram por mutação e não por revisão: o que um `@Composable` desenha, e o número do tempo limite.
- [x] 7.3 Rodar `openspec validate slice-4a-zero-device-auth --strict`. Verde: `Change 'slice-4a-zero-device-auth' is valid`.
- [x] 7.4 Conferir que `packages/domain`, `apps/api` e `apps/web` não foram tocados, que nenhuma spec existente mudou, e que o `assets.open` continua onde está — ele sai na 4a, e não aqui. Conferido contra `origin/main`: zero arquivos nos três módulos, zero specs existentes alteradas, `assets.open` intacto em `ScanActivity.kt:66` e nenhum arquivo de `scan/` tocado — só a declaração dele no manifesto.
